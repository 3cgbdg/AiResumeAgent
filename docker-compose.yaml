
services:
  redisdb:
    image: redis:latest
    container_name: redisdb
    ports:
      - 6379:6379
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  redis_data:
    